import {ForecastToGraphModel} from '../../src/models';

const forecastToGraphModel: ForecastToGraphModel = {
  labels: ['a', 'b', 'c', 'd', 'e'],
  data: [1, 2, 3, 4, 5],
};

export default forecastToGraphModel;
